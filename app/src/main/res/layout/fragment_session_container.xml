<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:tools="http://schemas.android.com/tools"
>

    <data>
        <import type="android.view.View"/>

        <variable
                name="viewModel"
                type="iclaude.festivaleconomia2019.ui.sessions.SessionContainerViewModel"/>
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:context=".ui.sessions.SessionContainerFragment"
            android:id="@+id/coordinatorLayout">

        <include layout="@layout/fragment_session_container_appbar"/>

        <androidx.viewpager.widget.ViewPager
                android:id="@+id/viewPager"
                android:layout_width="match_parent"
                android:layout_height="match_parent" android:layout_marginTop="112dp">
        </androidx.viewpager.widget.ViewPager>

        <ImageView
                android:layout_width="100dp"
                android:layout_height="150dp" app:srcCompat="@drawable/event_logo"
                android:id="@+id/imageView2"
                android:layout_gravity="center"
                android:visibility="@{viewModel.dataLoadedObs? View.GONE : View.VISIBLE}"/>

        <ProgressBar
                android:id="@+id/pbLoading"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:indeterminate="true"
                android:indeterminateTint="@color/light_grey"
                android:visibility="@{viewModel.dataLoadedObs? View.GONE : View.VISIBLE}"/>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

</layout>